import "server-only";

export const serverSideFunction = () => {
  console.log(`use multiple libraries,
use environment| variables,
Interact with a barabase,
process confidential information'`);
  return "server result";
};
